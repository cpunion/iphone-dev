prefix=@prefix@

all:		crt1.o dylib1.o bundle1.o

.PHONY:	install

install:    crt1.o dylib1.o bundle1.o
	@INSTALL@ -d $(DESTDIR)$(prefix)/lib
	@INSTALL@ crt1.o $(DESTDIR)$(prefix)/lib
	@INSTALL@ dylib1.o $(DESTDIR)$(prefix)/lib
	@INSTALL@ bundle1.o $(DESTDIR)$(prefix)/lib

